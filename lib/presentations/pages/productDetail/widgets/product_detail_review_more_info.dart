import 'package:flutter/material.dart';

import '../../../../utils/constants/app_assets.dart';

class ProductDetailReviewMoreInfo extends StatelessWidget {
  const ProductDetailReviewMoreInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(AppAssets.reviewImage);
  }
}
